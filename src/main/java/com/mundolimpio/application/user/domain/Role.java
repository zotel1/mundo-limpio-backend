package com.mundolimpio.application.user.domain;

/**
 * Enum que define los roles del sistema.
 * ADMIN: Gestion completa (materia prima, produccion, reportes)
 * OPERATOR: Solo ventas*/

public enum Role {
    ADMIN,
    OPERATOR
}
